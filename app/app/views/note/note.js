'use strict';

/* global app, Firebase */

// note controller and the modules we will want to use
app.controllers
  .controller('noteCtrl', ['$scope', '$routeParams', '$firebaseObject', 'Auth', function ($scope, $routeParams, $firebaseObject, Auth) {


}]);
